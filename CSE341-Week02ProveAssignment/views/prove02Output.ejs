<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <% include ./header.ejs %>
    </head>

    <body>
        <div class="container text-center">
            <form action="../" method="GET" style="position: sticky; top: 0;">
                <button type="submit" style="font-size: 2em; border-radius: 10px; margin: 1vw auto 0vw auto;">
                    Back</button>
            </form>
            <% if (books.length < 1) {%>
                <h3 style="margin: 2vw 3vw 1vw 3vw;">No books entered.</h3>
            <%} else {%>
                <% for (book of books) {%>
                    <h3 style="margin: 2vw 3vw 1vw 3vw;"><%= (book.title == "") ? "--- No Title Error ---" : book.title %></h3>
                    <p style="margin: 0.5vw 20vw 1vw 20vw;"><%= (book.summary == "") ? "--- No Summary Error ---" : book.summary %></p>
                    <hr style="width: 90vm; margin: 0vw 5vw 0vw 5vw; height: 0.3vw">
                <%}%>
            <%}%>
        </div>
    </body>
</html>